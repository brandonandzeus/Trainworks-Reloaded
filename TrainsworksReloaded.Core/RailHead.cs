namespace TrainsworksReloaded.Core;

public static class RailHead
{
    // private readonly List<Action<IConfigurationBuilder>> _configureBuildActions;
    // public void RegisterData<T, Configuration<T>>(IConfigurationBuilder configurationBuilder) where Configuration:
}
